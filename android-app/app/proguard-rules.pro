# Add project specific ProGuard rules here.
# TradeTrack Android app is a thin WebView wrapper; minification is
# disabled by default (see app/build.gradle), so this file is currently
# a placeholder for future use if minifyEnabled is turned on.
